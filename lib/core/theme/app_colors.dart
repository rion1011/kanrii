import 'package:flutter/material.dart';

abstract class AppColors {
  // Backgrounds
  static const background = Color(0xFF121212);
  static const surface = Color(0xFF1E1E1E);
  static const surfaceVariant = Color(0xFF2A2A2A);
  static const card = Color(0xFF242424);

  // Text
  static const textPrimary = Color(0xFFE8E8E8);
  static const textSecondary = Color(0xFF9E9E9E);
  static const textDisabled = Color(0xFF616161);

  // Accent (muted dark blue-grey, no neon)
  static const accent = Color(0xFF546E7A);
  static const accentLight = Color(0xFF78909C);

  // Semantic
  static const divider = Color(0xFF2E2E2E);
  static const checkboxActive = Color(0xFF607D8B);
  static const deleteRed = Color(0xFF8B3A3A);
  static const cardBorder = Color(0xFF333333);
}
